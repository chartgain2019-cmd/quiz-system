# ===========================================
# 🚀 نظام الاختبارات التفاعلي - إعدادات البيئة
# ===========================================

# 🔐 الأمان والمفاتيح
JWT_SECRET=quiz-system-super-secure-key-2024-with-special-chars@#$%^&*
JWT_EXPIRES_IN=7d
NODE_ENV=production

# 🌐 إعدادات السيرفر
PORT=3000
HOST=0.0.0.0
CLIENT_URL=https://your-frontend-domain.com

# 🔗 إعدادات CORS (للسماح بالاتصال من النطاقات المسموحة)
CORS_ORIGIN=https://your-frontend-domain.com,http://localhost:3000,http://127.0.0.1:3000
CORS_CREDENTIALS=true

# ⚡ إعدادات الأداء
REQUEST_LIMIT=10mb
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100

# 📊 إعدادات قاعدة البيانات (للمستقبل إذا أضفت قاعدة بيانات حقيقية)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=quiz_system
# DB_USER=quiz_user
# DB_PASSWORD=strong-db-password

# 📧 إعدادات البريد الإلكتروني (للمستقبل إذا أضفت إشعارات)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# 🔍 إعدادات التصحيح
DEBUG=false
LOG_LEVEL=info
